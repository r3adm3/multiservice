version: '3'

services:
  addsvc:
    build: './addService'
    ports:
      - "18081:80"
  minussvc:
    build: './minusService'
    ports:
      - "18082:80"
  multiplysvc:
    build: './multiplyService'
    ports:
      - "18083:80"
  frontend:
    build: './frontEnd'
    ports:
      - "18080:80"